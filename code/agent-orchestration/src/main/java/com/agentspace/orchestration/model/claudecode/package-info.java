/**
 * Claude Code SDK {@code stream-json} 的消息模型（envelope / inner message / content block），
 * 由 {@code client.claudecode} 适配层解析。未知字段忽略以兼容 SDK 演进。
 */
package com.agentspace.orchestration.model.claudecode;
